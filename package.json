{
  "name": "ntpu-ai-cloudflare-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:check": "wrangler deploy --dry-run",
    "test": "node --test",
    "check": "npm run test && npm run deploy:check"
  },
  "devDependencies": {
    "wrangler": "4.120.1"
  },
  "dependencies": {
    "fflate": "^0.8.2",
    "opencc-js": "^1.4.1"
  }
}
